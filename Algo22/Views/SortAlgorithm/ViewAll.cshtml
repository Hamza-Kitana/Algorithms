@model List<Details>



    <html>
<head>
    <link href="~/css/AllV.css" rel="stylesheet" />
</head>
<body>
    <h1> <span class="yellow">Sort ALgorthim Values</pan></h1>
   
Array Not Sorted
    <table class="tab" border="2">
        <tr>
            @foreach (var x in Model[0].Array2)
            {
                <td class="tdd">@x</td>
            }

        </tr>
        <tr>
            @foreach (var c in Model[0].Array)
            {
                <td class="tdd">@c</td>
            }
        </tr>
        <tr>
            @for (int i = 0; i < Model[0].Array.Length; i++)
            {
                <td class="tdd tw">@i</td>
            }
        </tr>
    </table>



    <table class="container">
        <tr>
            <th>Name algorthim</th>
            <th>time</th>
            <th>cabsity</th>
            <th>N</th>
        </tr>
        @foreach (var d in Model)
        {
            <tr>
                <td>@d.sorted</td>
                <td>@d.Time</td>
                <td>@d.MemoryUsed</td>
                <td>@d.N</td>
            </tr>
        }

    </table>
</body>


</html>